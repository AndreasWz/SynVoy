/*
 * Compatibility stub.
 *
 * The docker_max profile now keeps its strict-syntax-compatible resource
 * config inside the profile closure in nextflow.config. This file is kept so
 * older notes or user commands that reference it do not fail with a missing
 * include.
 */
